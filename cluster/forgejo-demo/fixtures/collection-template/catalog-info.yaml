---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${REPO_NAME}
  description: ${REPO_DESCRIPTION}
  annotations:
    backstage.io/source-location: url:__FORGEJO_URL__/${REPO_OWNER}/${REPO_NAME}
  links:
    - url: __FORGEJO_URL__/${REPO_OWNER}/${REPO_NAME}
      title: Forgejo repository
      icon: scm
spec:
  type: ansible-collection
  lifecycle: experimental
  owner: user:default/demo-agent

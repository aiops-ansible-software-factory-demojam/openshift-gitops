apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: keycloak-demo
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  host: keycloak.apps.cluster-spz55.dyn.redhatworkshops.io
  to:
    kind: Service
    name: keycloak-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
